/**
 * @fileoverview Global configuration exports for Payload CMS.
 *
 * Globals are singleton configurations that apply site-wide.
 * Unlike collections, there is only one instance of each global.
 *
 * @module Globals
 */

export { default as SiteSettings } from './SiteSettings'
